<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=210mm, height=148.5mm" />
  <title>Phiếu Đề Nghị Chi Tạm Ứng</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Global box-sizing for consistent layout */
    * {
      box-sizing: border-box;
    }

    /* Page settings for A5 landscape print */
    @page {
      size: A5 landscape;
      margin: 10mm;
    }

    /* Base styles for HTML and Body, centering content */
    html, body {
      width: 210mm;
      height: 148.5mm;
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      font-size: 13px;
      background-color: #f5f5f5; /* Light grey background for screen */
      display: flex;
      justify-content: center;
      align-items: center;
    }

    /* Container for the form content */
    .container {
      width: 100%;
      height: 100%;
      background: white; /* White background for the form itself */
      padding: 10mm;
      box-shadow: 0 0 1px rgba(0, 0, 0, 0.1); /* Subtle shadow for screen display */
      display: flex;
      flex-direction: column;
      justify-content: space-between; /* Distribute space between header/content and signatures */
    }

    /* Print specific styles to remove background and shadow */
    @media print {
      html, body {
        background: white;
      }
      .container {
        box-shadow: none; /* Remove shadow when printing */
        margin: 0;
        padding: 10mm;
      }
    }

    /* Header section styling */
    .header {
      display: flex;
      align-items: center;
      gap: 15px;
      margin-bottom: 10px;
    }

    /* Logo image styling within header */
    .header-logo img {
      width: 50px;
      height: auto;
    }

    /* Company information styling within header */
    .header-company-info {
      text-align: left;
    }

    /* Section title styling (e.g., "PHIẾU ĐỀ NGHỊ CHI TẠM ỨNG") */
    .section-title {
      font-size: 1.25rem; /* Larger font size */
      font-weight: 700; /* Bold font weight */
      margin-bottom: 5px;
      color: #1f2937; /* Dark grey text */
      text-align: center;
    }

    /* Grid for main information sections */
    .info-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr); /* Two equal columns */
      gap: 10px; /* Gap between grid items */
      margin-bottom: 10px;
    }

    /* Row for grouping related info items (e.g., Name, MNV, Department) */
    .info-group-row {
      display: flex;
      gap: 10px;
      flex-wrap: wrap; /* Allow items to wrap on smaller screens/print areas */
      width: 100%;
    }

    /* Styling for information labels (e.g., "Họ tên:") */
    .info-label {
      font-weight: 600; /* Semi-bold */
      color: #4b5563; /* Medium grey */
    }

    /* Styling for information values */
    .info-value {
      color: #374151; /* Darker grey */
    }

    /* Makes an info grid item span all columns */
    .full-width-item {
      grid-column: 1 / -1; /* Spans from the first column line to the last */
    }

    /* Grid for signature section */
    .signature-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr); /* Four equal columns for signatures */
      gap: 10px;
      text-align: center;
      font-size: 0.875rem; /* Smaller font for signatures */
      margin-top: 10px;
    }

    /* Styling for signature roles (e.g., "Người đề nghị") */
    .signature-text {
      font-weight: 600; /* Semi-bold */
      color: #1f2937; /* Dark grey text */
    }
  </style>
</head>
<body>
  <div class="container">
    <div>
      <div class="header">
        <div class="header-logo">
          <img src="https://drive.google.com/thumbnail?id=12VL0Dxkj5d8xwLFVRslpHdtlWd4MHF5a" alt="Logo Công ty" class="rounded-full" />
        </div>
        <div class="header-company-info">
          <p class="text-sm uppercase font-bold text-gray-800">CÔNG TY TNHH VINACHI VIỆT NAM</p>
          <p class="text-gray-600 text-sm italic">Hương Mạc - Phù Khê - Từ Sơn - Bắc Ninh</p>
        </div>
      </div>

      <!-- Current date, dynamically set by 'ngay' -->
      <p class="text-right text-gray-500 text-sm italic mb-1"><?!= ngay ?></p>

      <!-- Form title, dynamically set by 'loai_phieu' -->
      <div class="section-title">PHIẾU ĐỀ NGHỊ <?!= loai_phieu ?></div>
      <!-- Form ID, dynamically set by 'id' -->
      <p class="text-center text-sm font-normal text-gray-500 mb-2">(Mã phiếu: <?!= id ?>)</p>

      <!-- Standard greeting to the finance department -->
      <p class="text-center text-sm font-semibold text-gray-700 mb-4">Kính gửi: Phòng Tài chính Kế toán</p>

      <!-- Grid for detailed information -->
      <div class="info-grid">
        <!-- Employee personal information -->
        <div class="full-width-item info-group-row">
          <div><span class="info-label">Họ tên:</span> <span class="info-value"><?!= ho_va_ten ?></span></div>
          <div><span class="info-label">MNV:</span> <span class="info-value"><?!= mnv ?></span></div>
          <div><span class="info-label">Phòng:</span> <span class="info-value"><?!= phong ?></span></div>
        </div>

        <!-- Request and content details -->
        <div><span class="info-label">Thời gian đề nghị:</span> <span class="info-value"><?!= tg_de_nghi ?></span></div>
        <div><span class="info-label">Thời gian cần có:</span> <span class="info-value"><?!= tg_can_co ?></span></div>
        <div><span class="info-label">Nội dung đề nghị:</span> <span class="info-value"><?!= nd_de_nghi ?></span></div>
        <div><span class="info-label">Khoản mục:</span> <span class="info-value"><?!= khoan_muc ?></span></div>

        <!-- Financial details -->
        <div class="full-width-item">
          <span class="info-label">Tổng chi phí đề xuất:</span>
          <span class="info-value text-red-600 font-bold"><?!= tong_cp ?> VNĐ</span>
        </div>
        <div class="full-width-item">
          <span class="info-label">Tổng tiền thanh toán hiện tại:</span>
          <span class="info-value text-red-600 font-bold"><?!= so_tien ?> VNĐ</span>
          <span class="info-value italic ml-2">(<?!= bang_chu ?>)</span>
        </div>
        <div class="full-width-item">
          <span class="info-label">Hình thức thanh toán:</span>
          <span class="info-value"><?!= hinh_thuc ?></span>
        </div>
        <div class="full-width-item">
          <span class="info-label">Thông tin chuyển khoản:</span>
          <span class="info-value"><?!= tt_thanh_toan ?></span>
        </div>
        <div class="full-width-item">
          <span class="info-label">Ghi chú:</span>
          <span class="info-value"><?!= ghi_chu ?></span>
        </div>
      </div>
    </div>

    <!-- Signature section -->
    <div class="signature-grid">
      <div><p class="signature-text">Người đề nghị</p></div>
      <div><p class="signature-text">Quản lý</p></div>
      <div><p class="signature-text">Kế toán</p></div>
      <div><p class="signature-text">Phê duyệt</p></div>
    </div>
  </div>
</body>
</html>
